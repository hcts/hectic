- form_for host do |form|
  = form.error_messages

  .fields
    .field
      = form.label :name
      = form.text_field :name
  .buttons
    .button
      = form.submit 'Save this Host'
      - unless current_page?('/')
        or
        = link_to 'Cancel', root_path
